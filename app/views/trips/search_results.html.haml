/ MH 4/16/13 Display search results of all matching trips from the search query, for a variety of rivers 
/ Partial gets river_id info separately for each trip as they may vary, so all's good 

%h1 Listing Trip Search Results

= render :partial => 'listtrips'
